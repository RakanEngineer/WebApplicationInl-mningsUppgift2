@inherits Umbraco.Web.Mvc.UmbracoViewPage<ContentModels.Home>
@using ContentModels = Umbraco.Web.PublishedModels;
@{
    Layout = "Master.cshtml";
}

<!--Hero -->
@Html.Partial("Hero")
<!-- Call To Action-->
@Html.Partial("Call To Action")

@*@Model.Value("helloWorldText")</h1>
@Model.Value("pageContentText")</h2>*@

<div class="container">
    <div class="row">
        <div class="col">
            <!-- Latest News Cards-->
            @Html.Partial("Latest News")
        </div>
    </div>
</div>

<!-- Latest News Articles -->
@*@{
    var selection = Umbraco.Content(Guid.Parse("64ddd3a3-e2ad-4929-a805-9455d4fcb1c0"))
    .ChildrenOfType("newsArticle")
    .Where(x => x.IsVisible())
    .OrderByDescending(x => x.CreateDate)
    .Take(3);
}
<ul>
    @foreach (var item in selection)
    {
        <li>
            <a href="@item.Url">@item.Name</a>
        </li>
    }
</ul>*@

